name: Android Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract Android project
        shell: bash
        run: |
          set -e

          ZIP_FILE="MadarsaApp_Supabase_Connected.zip"

          rm -rf .project_extract
          mkdir -p .project_extract

          unzip -q "$ZIP_FILE" -d .project_extract

          echo "Extracted project:"
          find .project_extract -maxdepth 6 -type f | sort

          shopt -s dotglob
          cp -a .project_extract/. .

          rm -rf .project_extract
          rm -f local.properties

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Debug APK
        run: gradle assembleDebug --stacktrace

      - name: Create source archive
        shell: bash
        run: |
          set -e

          rm -rf /tmp/madarsa_source
          mkdir -p /tmp/madarsa_source

          cp -a . /tmp/madarsa_source/project

          rm -rf /tmp/madarsa_source/project/.git
          rm -rf /tmp/madarsa_source/project/.gradle
          rm -rf /tmp/madarsa_source/project/app/build

          cd /tmp/madarsa_source

          zip -qr /tmp/madarsa-source-project.zip project \
            -x "project/MadarsaApp_Supabase_Connected.zip"

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: madarsa-debug-apk
          path: "**/build/outputs/apk/debug/*.apk"

      - name: Upload Android source
        uses: actions/upload-artifact@v4
        with:
          name: madarsa-source-project
          path: /tmp/madarsa-source-project.zip
